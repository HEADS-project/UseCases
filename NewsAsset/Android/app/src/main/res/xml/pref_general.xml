<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="tabbed_navigation"
        android:title="@string/pref_title_tabbed_navigation"
        android:summary="@string/pref_description_tabbed_navigation"
        android:defaultValue="true" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="location_update_frequency"
        android:title="@string/pref_title_location_update_frequency"
        android:defaultValue="60"
        android:entries="@array/pref_location_update_frequency_titles"
        android:entryValues="@array/pref_location_update_frequency_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
