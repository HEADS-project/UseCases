package no.tellu.tracker.heads;

import no.tellu.android.app.system.MainActivity;
import android.os.Bundle;

/**
 * A MainActivity for the HEADS sensor application, acting
 * as GUI container and entry point. This is referenced in
 * the manifest file.
 * 
 * @author Lars Thomas Boye, Tellu AS
 */
public class HeadsActivity extends MainActivity {

	@Override
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
	}
}
